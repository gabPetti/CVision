# Configuration for Supabase Edge Functions (Python)

name = "generate-cv-pdf"
version = "1.0.0"
description = "Gera PDF otimizado de Currículo Vitae a partir de HTML"

[env]
ENVIRONMENT = "production"

[runtime]
language = "python"
